[project]
name = "mp-events-hub"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "bcrypt>=5.0.0",
    "pre-commit>=4.3.0",
    "uuid-utils>=0.11.1",
]
